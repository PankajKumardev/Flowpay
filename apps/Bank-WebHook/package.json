{
  "name": "bank-webhook",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx esbuild ./src/index.ts --bundle --platform=node --outfile=dist/index.js && npm run db:generate && npm run db:deploy",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start",
    "db:generate": "cd ../../packages/db && npx prisma generate",
    "db:migrate": "cd ../../packages/db && npx prisma migrate dev --name migrate",
    "db:deploy": "cd ../../packages/db && npx prisma migrate deploy",
    "prisma:generate": "npx prisma generate"
  },
  "dependencies": {
    "@repo/db": "workspace:*",
    "@types/express": "^4.17.21",
    "esbuild": "^0.20.2",
    "express": "^4.19.1",
    "zod": "^3.23.8",
    "@prisma/client": "^4.10.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "prisma": "^4.10.0"
  }
}
